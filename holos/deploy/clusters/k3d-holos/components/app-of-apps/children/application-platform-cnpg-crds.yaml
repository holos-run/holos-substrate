apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "12"
  labels:
    app.kubernetes.io/component: cnpg-crds
    app.kubernetes.io/name: platform-cnpg-crds
    app.kubernetes.io/part-of: platform
  name: platform-cnpg-crds
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  project: platform
  source:
    path: clusters/k3d-holos/components/cnpg-crds
    repoURL: oci://quay.holos.internal/holos/holos-substrate-config
    targetRevision: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - ServerSideApply=true
    - CreateNamespace=false
