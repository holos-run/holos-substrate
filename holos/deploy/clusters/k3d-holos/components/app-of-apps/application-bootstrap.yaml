apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    annotations:
        argocd.argoproj.io/sync-wave: "-1"
    labels:
        app.kubernetes.io/name: platform-bootstrap
        app.kubernetes.io/part-of: platform
    name: platform-bootstrap
    namespace: argocd
spec:
    destination:
        namespace: argocd
        server: https://kubernetes.default.svc
    project: platform
    source:
        directory:
            recurse: true
        path: clusters/k3d-holos/components/app-of-apps/children
        repoURL: oci://quay.holos.internal/holos/holos-substrate-config
        targetRevision: dev
    syncPolicy:
        automated:
            prune: true
            selfHeal: true
        syncOptions:
            - ServerSideApply=true
            - CreateNamespace=false
