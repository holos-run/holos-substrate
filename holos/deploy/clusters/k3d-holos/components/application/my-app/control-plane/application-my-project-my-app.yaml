apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  annotations:
    kargo.akuity.io/authorized-stage: my-project:my-app-config
  labels:
    app.kubernetes.io/name: my-app
  name: my-project-my-app
  namespace: argocd
spec:
  destination:
    namespace: my-project
    server: https://kubernetes.default.svc
  project: my-project
  source:
    path: .
    repoURL: oci://quay.holos.localhost/my-project/my-app-config
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=false
