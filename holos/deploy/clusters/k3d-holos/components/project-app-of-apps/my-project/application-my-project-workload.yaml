apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    labels:
        app.kubernetes.io/component: workload
        app.kubernetes.io/name: my-project-workload
        app.kubernetes.io/part-of: my-project
    name: my-project-workload
    namespace: argocd
spec:
    destination:
        namespace: argocd
        server: https://kubernetes.default.svc
    project: projects
    source:
        directory:
            include: '**/workload/**'
            recurse: true
        path: clusters/k3d-holos/components
        repoURL: oci://quay.holos.internal/holos/my-project-config
        targetRevision: dev
    syncPolicy:
        automated:
            prune: true
            selfHeal: true
        syncOptions:
            - ServerSideApply=true
            - CreateNamespace=false
